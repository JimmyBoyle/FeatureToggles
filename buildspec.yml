version: 0.2
phases:
  install:
    commands:
    - make init
  build:
    commands:
    - make compile-app
    - make test
    - make deploy
  post-build:
    commands:
    - make deploy
    - make teardown
